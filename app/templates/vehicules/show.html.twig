{% extends 'base.html.twig' %}

{% block title %}Vehicules{% endblock %}

{% block body %}
   <header>

   </header>
    <main>
        <div class="row">
            <div class="col-12 col-md-6">
            <article  class="col-12 col-md-6 col-lg-3 d-flex flex-column gap-3 position-relative mb-5 rounded-3 border border-warning shadow p-1">
                <img src="{{ asset('uploads/vehicules/' ~ vehicule.images) }}" alt="{{ vehicule.nom }}" class="align-middle rounded-3 w-100">
            </article>
        </div>
        </div>

    </main>
    <footer>

    </footer>
{% endblock %}
